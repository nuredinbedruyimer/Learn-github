// auth.js
export function login(userName) {
  processedName = userName.toUpperCase();
  trimmedName = processedName.trim();
  //  this is the line after the trimming
  for (char of processedName) {
    if (chat) {
      return trimmedName;
    } else {
      return processedName;
    }
  }
}
