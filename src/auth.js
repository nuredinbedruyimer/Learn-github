// auth.js
export function login(userName) {
  processedName = userName.toUpperCase().trim();
  //  this is the comment i have added to check
  //  wether conflict will emerge or not

  return processedName;
}
